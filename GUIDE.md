
# 📚 راهنمای جامع نصب و استفاده

این راهنما شامل تمام مراحل لازم برای اجرای برنامه روی کامپیوتر شخصی، استفاده در موبایل و راه‌اندازی آنلاین روی سرورهای ابری (Cloudflare) است.

---

## 🛠️ پیش‌نیازها و لینک‌های دانلود

قبل از شروع، ابزارهای زیر را دانلود و نصب کنید:

| ابزار | توضیحات | لینک دانلود |
| :--- | :--- | :--- |
| **Node.js** | برای اجرای کدهای جاوااسکریپت (نسخه LTS پیشنهاد می‌شود) | [دانلود Node.js](https://nodejs.org/en/download/) |
| **VS Code** | ویرایشگر کد (اختیاری، برای ویرایش پروژه) | [دانلود VS Code](https://code.visualstudio.com/) |
| **Git** | برای دریافت پروژه از گیت‌هاب | [دانلود Git](https://git-scm.com/downloads) |
| **Google AI Studio** | برای دریافت کلید رایگان هوش مصنوعی | [دریافت API Key](https://aistudio.google.com/app/apikey) |
| **GitHub** | برای ذخیره پروژه (جهت اتصال به کلودفلر) | [سایت GitHub](https://github.com/) |
| **Cloudflare** | برای میزبانی رایگان سایت | [سایت Cloudflare](https://dash.cloudflare.com/sign-up) |

---

## 💻 ۱. راهنمای نصب و اجرا روی کامپیوتر (Local)

1.  **دریافت پروژه:**
    *   اگر فایل زیپ دارید، آن را اکستراکت (Extract) کنید.
    *   یا با دستور زیر از گیت‌هاب کلون کنید:
        ```bash
        git clone [لینک-ریپوزیتوری-شما]
        ```

2.  **نصب وابستگی‌ها:**
    *   پوشه پروژه را باز کنید.
    *   کلید `Shift` را نگه دارید و کلیک راست کنید، سپس `Open PowerShell window here` یا `Open in Terminal` را بزنید.
    *   دستور زیر را بنویسید و اینتر بزنید:
        ```bash
        npm install
        ```

3.  **تنظیم کلید هوش مصنوعی:**
    *   یک فایل جدید در کنار فایل `package.json` بسازید و نام آن را `.env` بگذارید.
    *   کد زیر را درون آن کپی کنید و جای `YOUR_KEY` کلید دریافتی از گوگل را بگذارید:
        ```
        API_KEY=AIzaSyDxxxxxxxxxxxxxxxxxxxxxxxx
        ```

4.  **اجرای برنامه:**
    *   دستور زیر را در ترمینال اجرا کنید:
        ```bash
        npm run dev
        ```
    *   آدرس نمایش داده شده (مثلاً `http://localhost:5173`) را در مرورگر باز کنید.

---

## 📱 ۲. راهنمای استفاده روی موبایل (Android & iOS)

این برنامه یک وب‌اپلیکیشن است. پس از اینکه آن را روی کلودفلر (مرحله ۳) نصب کردید، می‌توانید مانند یک اپلیکیشن بومی روی گوشی نصب کنید:

### اندروید (Chrome)
1.  سایت را در مرورگر کروم باز کنید.
2.  روی سه نقطه بالا سمت چپ ضربه بزنید.
3.  گزینه **Install App** یا **Add to Home screen** را انتخاب کنید.
4.  آیکون برنامه به لیست اپلیکیشن‌های شما اضافه می‌شود.

### آیفون (Safari)
1.  سایت را در سافاری باز کنید.
2.  روی دکمه **Share** (مربع با فلش رو به بالا) ضربه بزنید.
3.  گزینه **Add to Home Screen** را انتخاب کنید.
4.  دکمه **Add** را بزنید.

---

## ☁️ ۳. راهنمای نصب روی کلودفلر (Cloudflare Pages)

این روش بهترین راه برای آنلاین کردن سایت به صورت **رایگان و دائمی** است.

### الف) آماده‌سازی فایل‌ها
یک پوشه به نام `deployment` در پروژه وجود دارد (اگر نیست بسازید) و فایل‌های مورد نیاز داخل آن قرار دارد. مهمترین فایل برای کلودفلر `_redirects` است که باعث می‌شود صفحات داخلی (مثل `/trial`) هنگام رفرش شدن ارور ندهند.

*   **نکته:** محتویات پوشه `deployment/cloudflare` باید هنگام بیلد شدن به پوشه نهایی (`dist`) منتقل شوند. (این کار معمولا خودکار انجام نمی‌شود مگر تنظیم کنید، اما ساده‌ترین راه آپلود مستقیم یا اتصال به گیت‌هاب است).

### ب) روش اتصال به گیت‌هاب (پیشنهادی)
1.  پروژه خود را در سایت [GitHub.com](https://github.com) آپلود (Push) کنید. (فایل `.env` را آپلود نکنید).
2.  وارد داشبورد [Cloudflare Pages](https://dash.cloudflare.com/) شوید.
3.  روی **Create a project** > **Connect to Git** کلیک کنید.
4.  ریپوزیتوری خود را انتخاب کنید.
5.  در بخش تنظیمات Build:
    *   **Framework preset:** گزینه `Vite` یا `React` را انتخاب کنید.
    *   **Build command:** `npm run build`
    *   **Output directory:** `dist`
6.  **مهم:** به بخش **Environment variables** بروید و متغیر زیر را اضافه کنید:
    *   Variable name: `API_KEY`
    *   Value: (کلید هوش مصنوعی گوگل خود را اینجا وارد کنید)
7.  دکمه **Save and Deploy** را بزنید.

### ج) روش آپلود دستی (بدون گیت‌هاب)
1.  روی کامپیوتر دستور `npm run build` را اجرا کنید.
2.  یک پوشه جدید به نام `dist` ساخته می‌شود.
3.  فایل `deployment/cloudflare/_redirects` را کپی کرده و داخل پوشه `dist` قرار دهید.
4.  در داشبورد Cloudflare Pages، گزینه **Direct Upload** را انتخاب کنید.
5.  پوشه `dist` را بکشید و رها کنید.
6.  **توجه:** در این روش چون فایل `.env` در بیلد نیست، باید مطمئن شوید کد شما طوری نوشته شده که اگر کلید نبود کار کند یا کلید را در کد (با ریسک امنیتی) قرار دهید. روش گیت‌هاب امن‌تر است.

---

## 📂 فایل‌های مورد نیاز نصب (Cloudflare)

فایل زیر در مسیر `deployment/cloudflare/_redirects` ایجاد شده است. این فایل به کلودفلر می‌گوید تمام درخواست‌ها را به `index.html` بفرستد تا React Router کار کند.

**محتوای فایل `_redirects`:**
```
/*  /index.html  200
```

---

## ⚠️ نکات مهم امنیتی
*   **کلید API:** هرگز کلید API خود را در گروه‌های عمومی تلگرام یا مخازن عمومی گیت‌هاب منتشر نکنید.
*   **هزینه:** استفاده از مدل `gemini-pro` تا حدودی رایگان است، اما اگر ترافیک بالایی دارید، پنل گوگل خود را چک کنید.
